input_file: data.csv
output_method: console
aggregate:
  area: state
    state: GROUP(counties)
    region: GROUP(states)
  fields:
    - cases
    - deaths
    - recoveries
    - mortality_rate
    - recovery_rate
top_n: 15
most_least: most
rank_field: cases